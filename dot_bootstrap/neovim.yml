- name: Install pakcage neovim
  kewlfft.aur.aur:
    name: neovim

- name: Install nvim-treesitter dependencies (tree-sitter, tree-sitter-cli)
  kewlfft.aur.aur:
    name:
      - tree-sitter
      - tree-sitter-cli

- name: Install lazy.nvim dependencies (luarocks)
  kewlfft.aur.aur:
    name: luarocks

- name: Install mason.nvim dependencies (composer, docker, julia, go, nodejs, php, python, python-pip, ruby, rubygems, rustup)
  kewlfft.aur.aur:
    name:
      - composer
      - docker
      - julia
      - go
      - nodejs
      - php
      - python
      - python-pip
      - ruby
      - rubygems
      - rustup

- name: Install yazi.nvim dependencies (yazi)
  kewlfft.aur.aur:
    name: yazi

- name: Install telescope.nvim dependencies (ripgrep, fd)
  kewlfft.aur.aur:
    name:
      - ripgrep
      - fd

- name: Install telescope-fzf-native.nvim dependencies (cmake, fzf)
  kewlfft.aur.aur:
    name:
      - fzf
      - cmake

- name: Install codecompanion.nvim dependencies (github-cli)
  kewlfft.aur.aur:
    name: github-cli

- name: Install TexLab LSP dependencies (texlive-binextra, perl-yaml-tiny, perl-file-homedir)
  kewlfft.aur.aur:
    name:
      - texlive-binextra
      - perl-yaml-tiny
      - perl-file-homedir

- name: Install vimtex dependencies (biber, xdotool)
  kewlfft.aur.aur:
    name:
      - biber
      - xdotool
